services:
  backend:
    image: amarbhise/intbuddy-backend:latest
    container_name: intbuddy-backend

    restart: unless-stopped

    env_file:
      - ../env/backend.env

    expose:
      - "8080"

    networks:
      - intbuddy-network

networks:
  intbuddy-network:
    external: true
